{
  "name": "ahgora-batidas-impares",
  "version": "1.8.1",
  "private": true,
  "description": "Painel local e modular para análise de batidas ímpares do TOTVS Ahgora.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:js": "find assets/js -name '*.js' -print0 | sort -z | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=18"
  }
}
